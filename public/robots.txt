# ER Marketing — robots policy
# Sitemap referenced absolutely so it works regardless of which host
# the bot is querying (apex vs www).

User-agent: *
Allow: /

# Block analytics endpoints from the crawl path — these are tracked
# server-side and don't need to be indexed.
Disallow: /api/

Sitemap: https://trafegomanaus.com.br/sitemap.xml

# llms.txt — emerging convention (https://llmstxt.org) for AI crawlers
# (ChatGPT, Claude, Perplexity, Gemini). Provides a curated, machine-
# friendly summary of the site for retrieval-augmented systems.
# Bots that respect the convention should fetch /llms.txt.
